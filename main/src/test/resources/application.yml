#mysql
spring:
  datasource:
    url: jdbc:h2:mem:db_h2;MODE=MYSQL;INIT=RUNSCRIPT FROM './src/test/resources/mall_permission.sql'
    driver-class-name: org.h2.Driver
    username: root
    password: root

# 打印sql
logging:
  level:
    com.xiongxin.boot.mapper: debug

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      table-prefix: u_